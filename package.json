{
  "name": "agent-infra",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --filter @agent-infra/core --filter @agent-infra/db --filter @agent-infra/runtime-ai-sdk build && pnpm --filter playground-web dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "@types/pg": "^8.15.5",
    "typescript": "^5.8.3"
  }
}

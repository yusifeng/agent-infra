{
  "name": "playground-fastify-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap:db": "tsx src/bootstrap-db.ts",
    "bootstrap:db:built": "node dist/bootstrap-db.js",
    "dev": "tsx watch src/server.ts",
    "dev:prepared": "NODE_ENV=development pnpm bootstrap:db && NODE_ENV=development pnpm dev",
    "dev:prepared:sqlite": "PLAYGROUND_DB_MODE=sqlite NODE_ENV=development tsx src/bootstrap-db.ts && PLAYGROUND_DB_MODE=sqlite NODE_ENV=development tsx watch src/server.ts",
    "dev:prepared:turso": "PLAYGROUND_DB_MODE=turso NODE_ENV=development tsx src/bootstrap-db.ts && PLAYGROUND_DB_MODE=turso NODE_ENV=development tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "start:prepared": "NODE_ENV=production pnpm bootstrap:db:built && NODE_ENV=production pnpm start",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-infra/app": "workspace:*",
    "@agent-infra/contracts": "workspace:*",
    "@agent-infra/db": "workspace:*",
    "@agent-infra/durable-chat-server": "workspace:*",
    "@agent-infra/runtime-pi": "workspace:*",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.0.0",
    "@fastify/rate-limit": "^10.3.0",
    "@mariozechner/pi-agent-core": "^0.66.1",
    "@sinclair/typebox": "^0.34.41",
    "argon2": "^0.44.0",
    "drizzle-orm": "^0.44.2",
    "fastify": "^5.0.0",
    "resend": "^6.12.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}

# Quick start (zero-config): do not set DATABASE_URL or OPENAI_API_KEY.
# Defaults:
# - AI_MODE=mock
# - SQLite file at ./local.db

AI_MODE=mock

# Optional: custom SQLite path when DATABASE_URL is not set.
# SQLITE_PATH=./local.db

# Optional: Advanced mode (real model)
# AI_MODE=real
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
#
# Optional: /pi experiment page can use provider keys configured inside pi-web-ui's own Settings/Keys UI.
# It does NOT reuse the vars above automatically and does NOT persist sessions in agent-infra DB.

# Optional: pi-narrow (DEV ONLY) preload DeepSeek key into browser storage.
# This will expose the key to the browser when PI_NARROW_ALLOW_ENV_KEYS=1.
# PI_NARROW_ALLOW_ENV_KEYS=0
# DEEPSEEK_API_KEY=sk-...

# Optional: Advanced mode (Postgres)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/agent_infra

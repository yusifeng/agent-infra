{
  "name": "@agent-infra/durable-chat-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-infra/contracts": "workspace:*",
    "@agent-infra/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
